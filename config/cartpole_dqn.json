{
  "num_iterations":20,
  "num_training_steps":20000,
  "min_replay_history":1000,
  "max_steps_per_episode":200,
  "update_period":1,
  "target_update_period":500,
  "checkpoint_freq_per_step":10000,
  "print_freq_per_episode":10,
  "replay_capacity":5000,
  "batch_size":32,
  "epsilon_decay_period":10000.0,
  "epsilon_train":0.02,
  "epsilon_eval":0.001
}